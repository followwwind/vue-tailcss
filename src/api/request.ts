// 服务匹配前缀
const SERVER_PRE = {
  
}

// 接口请求路径
const SERVER_URL = {
  // hypervisor
  user: '/api/user/', // 用户
  api: '/api/account/', // 账户
  assert: '/api/assert/' // 资产
}

export default SERVER_URL;